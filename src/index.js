console.log("hello world");

let a = 1, b = 2;

console.log("%d + %d = %d", a, b, a + b);

/**
 * @name add
 * @param {number} a 
 * @param {number} b 
 * @returns {number}
 */
function add(a, b) {
    return a + b
}
